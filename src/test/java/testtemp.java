import org.junit.Test;
import static org.junit.Assert.assertEquals;
import static org.mockito.Mockito.*;

public class testtemp {
    @Test
    public void testMessage() {
        assertEquals("message", "message");
    }

    @Test
    public void testMessageErr() {
        assertEquals("message", "message");
    }
}
